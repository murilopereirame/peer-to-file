{
  "name": "@p2f/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Platform-agnostic API client, types and theme tokens shared by the peer-to-file mobile (Expo) and desktop (Tauri) apps. Consumed as TypeScript source directly (no build step) via each app's bundler alias.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "license": "MIT"
}
